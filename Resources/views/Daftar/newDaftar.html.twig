{% block content %}
    <h1>Create Daftar Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_daftar') }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
